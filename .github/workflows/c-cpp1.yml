name: C/C++ CI

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      # 安装autotools编译依赖
      - name: Install build dependencies
        run: sudo apt update && sudo apt install -y autoconf automake libtool gcc g++ make

      # 生成 configure 脚本（关键新增步骤）
      - name: Generate configure
        run: autoreconf -i

      - name: configure
        run: ./configure

      - name: make
        run: make

      - name: make check
        run: make check

      - name: make distcheck
        run: make distcheck
